# Private NPM package for janus.js

### Publish package to private registry:

```
1. npm login --scope=@sw-stack --registry=https://npm.pkg.github.com

2. npm publish --scope=@sw-stack --registry=https://npm.pkg.github.com
```
